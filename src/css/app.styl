@import '~variables'
@import './maps'
@import './cards'

.q-list {
  color: white;
  background-color: $primary;
}

.q-list-header {
  color: white
}

.q-item-sublabel {
  color: white
}

.q-item-side {
  color: white
}

.street {
  font-family: streetFont;
  font-size: 20px;
  cursor: pointer;
}
