@import '~variables'
@import './maps'
@import './cards'

.q-layout {
}

.q-list {
  color: white;
  background-color: $primary;
}

.q-list-header {
  color: white
}

.q-item-label {
  color: blue
  text-transform: uppercase;
  font-weight:200;
}

.q-item-sublabel {
  color: white
  font-style: italic;
}

.q-item-side {
  color: white
}

.street {
  font-family: streetFont;
  font-size: 20px;
  cursor: pointer;
}

.urban {
  font-family: streetFont;
  color: $primary;
  font-size: 40px;
}

.q-toolbar-title {
  font-weight:200;
}
.q-toolbar-subtitle {
  font-family: streetFont;
}
